#include "main.h"

/**
* _isdigit - A function to check for digits (0 - 9)
* @c: Fucntion argument
* @a - return value
* Return: Returns an int
**/

int _isdigit(int c)
{
	if (c >= 48 && c <= 57)
	{
		int a = 1;

		return (a);
	}
	else
	{
		int a = 0;

		return (a);
	}
}
